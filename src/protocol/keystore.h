#ifndef MLDSA_AUTH_PROTOCOL_KEYSTORE_H
#define MLDSA_AUTH_PROTOCOL_KEYSTORE_H

/*
 * Trust-on-first-use (TOFU) public key pinning store (Section 6.2).
 * Re-registration of an existing client_id with a different public key
 * must be rejected and logged, never silently accepted (Security
 * Requirement 4.7). Scaffold only (Step 1); implemented alongside the
 * handshake state machine in Step 4.
 */

#endif /* MLDSA_AUTH_PROTOCOL_KEYSTORE_H */
