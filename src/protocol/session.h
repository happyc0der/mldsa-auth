#ifndef MLDSA_AUTH_PROTOCOL_SESSION_H
#define MLDSA_AUTH_PROTOCOL_SESSION_H

/*
 * Session layer (Section 6.4): sequence number tracking, replay/reorder
 * rejection, rekey triggers, send/recv over the AEAD channel. Scaffold
 * only (Step 1); implemented in Step 5.
 */

#endif /* MLDSA_AUTH_PROTOCOL_SESSION_H */
