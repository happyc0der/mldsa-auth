S1|^authd_e2e$|admin socket mode is not 0600
C1|^authd_e2e$|on a bad config exited
C2|^test_authd_cli$|check-config: admin reports the SAME status name the shared parser returned
K1|^test_authd_cli$|no plaintext MLDSASK file was written anywhere (Req 10)
K2|^authd_e2e$|server.ek opslimit is
P1|^test_authd_cli$|init: server.ek opens with the passphrase AS WRITTEN to the file
P2|^test_authd_cli$|init: creates a server key, a store and a passphrase file
L1|^test_authd_cli$|framing: the list table matches what the daemon actually sends
L2|^test_authd_cli$|framing: a list carries its rows between the header and END
E1|^test_authd_cli$|enroll-operator: a .pub whose embedded id differs from --handle is id-mismatch
I1|^test_authd_cli$|init: an EXISTING STORE is refused even when all key material is gone
R1|^authd_e2e$|the cleanup epilogue did not run
B1|^authd_e2e$|the backup is not 0600
